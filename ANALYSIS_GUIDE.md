# 掃描結果分析與報告產出指引

本文件供負責產出報告的大型語言模型（LLM）使用。LLM 先將單一掃描批次的原始輸出整理成內部分析報告，再由內部報告產出交付稿。分析結論只能來自本文件與該批次的掃描輸出，不得從專案內其他文件或網路資料補寫缺少的事實。

## 1. 規則層級

本文件使用以下三種規則層級：

- **必須**：不遵守會使範圍、證據或報告內容失真。
- **條件式**：只有規則列出的條件成立時才套用。
- **建議**：不影響正確性，但可改善一致性或可讀性。

同一規則同時出現在多處時，以「發現判定規則」為準。工具章節只說明如何讀取原始值，不另外定義判定狀態（`Status`）或風險等級（`Severity`）。

## 2. 分析範圍與輸入

### 2.1 批次邊界

以下規則必須遵守：

- 每份分析報告只處理一個 `RUN_ID`。
- 不得合併不同批次的工具輸出、掃描設定、目標或證據。
- 報告標題與所有未歸檔證據路徑都必須標示該 `RUN_ID`。
- `targets.txt` 是本次原定受測目標的完整集合。實際輸出只能用來判定各目標的完成狀態，不能取代 `targets.txt`。
- 缺少 `targets.txt` 時可以繼續分析現有輸出，但必須在內部報告的範圍核對結果，以及交付稿的限制事項中寫明「缺少 targets.txt，無法驗證受測目標是否完整」。
- 完全沒有 reachability、nmap、Nuclei 或 ZAP 等主要掃描輸出時，停止產出完整報告，只列出缺少的材料。

### 2.2 材料清單

開始分析前，先建立「存在、缺少、未執行」三種狀態的材料清單。

| 材料 | 用途 |
| --- | --- |
| `targets.txt` | 判定原定受測目標的完整集合 |
| `outputs/<RUN_ID>/reachability/reachability.txt`、`routes.txt` | 判斷網路連通性與路由 |
| `outputs/<RUN_ID>/nmap/open_ports.csv` | 判定哪些服務應進入 Web 掃描範圍；`service` 含 `http` 才納入 |
| `outputs/<RUN_ID>/run/web_scope.txt`、`outputs/<RUN_ID>/zap/scope.txt` | 核對 Web 掃描涵蓋範圍 |
| `outputs/<RUN_ID>/nmap/inventory_*`、`safe_*`、`vulners_*` | 讀取開放埠、服務、版本與 CVE 候選 |
| `outputs/<RUN_ID>/nuclei/nuclei.jsonl` | 讀取 Nuclei 命中；檔案缺席不代表沒有執行 |
| `outputs/<RUN_ID>/zap/zap_report.html`、`plan.yaml` | 讀取 ZAP 警示與掃描模式 |
| `outputs/<RUN_ID>/run/progress.log` | 讀取錯誤、警告與被略過目標 |
| `outputs/audit_<時間戳>/run/ubuntu_audit_*.tar.gz` | 有執行 SSH 主機稽核時，核對套件修補與監聽埠 |
| `outputs/clam_<時間戳>/run/ubuntu_clamav_*.tar.gz` 或委託單位提供的 EDR 輸出 | 有執行惡意程式檢測時，讀取引擎、特徵碼、掃描類型與偵測結果 |
| `env.md` | 取得主機名稱與角色；不得把其他機敏內容放入報告 |

材料缺少時必須遵守以下規則：

- 分析一律以 `outputs/<RUN_ID>/` 的原始輸出為材料，不接受跨 RUN_ID、或任何「主機／工具」重組後副本目錄作為分組依據。
- 原始輸出是單一檔案、未依主機預拆分時，分析端自行分組：Nuclei 依 `nuclei.jsonl` 每筆的 `host` 欄，ZAP 依報告 `Alert Detail` 每個 `instance` 的 URL。
- 工具未執行或沒有輸出時，標示「未執行」或「無法驗證」，不得寫成「沒有發現」。
- 只把本次實際執行且有輸出支持的工作列入檢測範圍。
- `env.md` 沒有提供角色時填「待確認」，不得依 IP、主機名稱或服務自行推測。

### 2.3 資產單位

- 每個實際掃描的 IP 各自建立一個主機章節。
- 網域名稱、rDNS 與角色是該 IP 的識別資訊，不取代 IP 這個資產單位。
- 同一網域解析到多個 IP 時，已掃描 IP 分別建立章節；未掃描 IP 列入整體限制事項。

## 3. 分析流程

分析必須依下列順序執行。工具判讀與獨立檢測來源不列為額外流程步驟，避免把時間順序與資料來源混在同一層。

### 3.1 核對範圍

1. 讀取 `targets.txt`，建立原定受測目標清單。
2. 對照 reachability、nmap、Web scope 與 `progress.log`，判定每個目標為「完成、部分完成、未回應、未執行或無法驗證」。
3. 記錄 DNS 多 IP、工具錯誤、被略過目標與未執行項目。

### 3.2 建立資產盤點

1. 依實際掃描 IP 建立主機章節。
2. 從 nmap 讀取開放埠、服務與版本。
3. 從 `env.md` 讀取主機名稱與角色。
4. 標示各服務是否進入 Web scope。

### 3.3 萃取候選發現

1. 從 Nuclei、ZAP、nmap 的 vulners 結果與 SSH 主機稽核輸出讀取候選發現，也就是尚待判定是否成立的工具命中。
2. 每筆候選發現先保留工具回報的風險等級、原始證據與受影響 IP。
3. 同一主機、同一問題的多個 URL 或命中位置（`instance`）合併成一項發現。各命中位置仍須保留定位資訊。

### 3.4 判定發現

1. 套用第 5 節的判定狀態、風險等級與複驗規則。
2. 記錄工具回報的風險等級、調整後的風險等級、調整理由與複驗狀態。
3. 依證據充分程度將候選分類為「發現、待複驗、observation 或排除」。

### 3.5 交叉核對

1. 在同一主機內核對不同工具的結果。
2. 在不同主機間找出同類問題，但每個 IP 仍建立不同 Finding ID。
3. 把無法解決的不一致與覆蓋缺口寫入限制事項。

### 3.6 產出報告

1. 先完成第 8 節的內部分析報告。
2. 再依第 9 節的納入規則與固定結構產出交付稿。
3. 不得直接從原始工具輸出跳過內部判定，產出交付稿。

## 4. 各來源的判讀方式

### 4.1 Reachability 與路由

- ping 全部遺失不能證明主機離線，可能是 ICMP 被封鎖。
- `Connection ... succeeded!` 可證明該服務埠在測試時可連線。
- failed 無法單獨區分關閉埠與防火牆靜默丟棄，應對照 nmap 的 `filtered` 狀態；仍無法判定時列入限制事項。

### 4.2 nmap

- `inventory_*` 提供開放埠與版本；service 尾端的 `?` 表示版本不確定。
- `safe_*` 補充產品與 `http-title` 等資訊。
- `vulners_*` 只有出現 `CVE-20xx-xxxx` 才形成 CVE 候選；沒有輸出不能推論安全。
- vulners 的 CVE 候選必須與 `safe_*` 顯示的產品版本吻合，才能成為正式發現；版本不足時列為待複驗或 observation，並說明缺少的版本證據。

### 4.3 Nuclei

- 讀取 `severity`、`template-id`、`host`、`url`、`matched-at`、`extracted-results`、`curl-command`、`request` 與 `response`。
- `matched-at` 指向第三方網域時，只能證明屬性或外部關聯，判定狀態標為 `observation`。
- 同一主機、同一 template 的多筆結果合併為一項發現，並保留所有命中位置。
- `progress.log` 出現大量 errors 或 `Skipped` 時，標示覆蓋不完整。

### 4.4 ZAP

- 從 `Summary of Alerts` 取得警示清單，再依 `Alert Detail` 中每個 `instance` 的 URL 判定受影響 IP 與路徑。
- 風險值對應為 `3 = High`、`2 = Medium`、`1 = Low`、`0 = Info`、`-1 = False Positive`。
- 同一警示影響多個 IP 時，每個 IP 只保留自己的命中位置，並建立不同 Finding ID。
- 先讀 `plan.yaml` 確認被動或主動模式。
- spider 取得的 URL 過少表示動態頁面可能未涵蓋，應列為覆蓋限制，不得解讀為沒有弱點。

### 4.5 `progress.log`

以下內容要轉成範圍狀態或限制事項：

- `Warning: Hostname X resolves to N IPs`。
- Nuclei errors 數量。
- `Skipped ... unresponsive permanently`。
- 其他會造成目標、路徑或工具未完成的警告。

ZAP 結束碼為 0 或 2 時，都視為工具已完成。報告仍須依警示與覆蓋資料判讀結果。

### 4.6 SSH 主機稽核

- 有 `ubuntu_audit_*.tar.gz` 時，用其中的套件與版本資料核對修補缺口，並以監聽埠資料交叉檢查 nmap。
- 沒有設定 SSH 稽核或沒有輸出時，標示「未執行」或「無法驗證」。
- 原始文件沒有定義壓縮檔內的固定檔案名稱與欄位。無法辨識時標示「待確認」，不得自行猜測。

### 4.7 惡意程式檢測

- 只接受本次 ClamAV 或委託單位提供的 EDR 輸出。
- 報告應包含引擎版本、特徵碼版本、掃描類型、偵測結果與證據路徑。
- 沒有輸出時不得寫成「未偵測到惡意程式」。此時應標示未執行或無法驗證，並移出本次實際檢測範圍。

### 4.8 工具版本

只列本次實際使用且有證據的工具：

| 工具 | 版本證據 |
| --- | --- |
| nmap | 掃描報告第一行的 `Nmap <版本>` |
| Nuclei | `nuclei -version` 輸出或 progress 日誌 |
| ZAP | `zap_report.html` 的 `ZAP Version:` |
| OpenVAS | gvm 版本資訊；缺少時標示待確認 |
| ClamAV | `clamscan --version` |
| EDR | 委託單位提供的引擎版本 |

原始文件沒有定義 Vuls、OpenVAS、Windows audit 或 Defender 的完整輸入格式與判讀規則。只有工具名稱或版本不足以形成發現；相關內容標示「待確認」。

## 5. 發現判定規則

### 5.1 三個獨立欄位

每項候選發現必須分別記錄風險等級（`Severity`）、判定狀態（`Status`）與複驗狀態（`Verification`）。三個欄位各自回答不同問題，不得互相代替。

#### 風險等級（`Severity`）

風險等級表示問題成立時會造成多大的影響：

- `Critical`
- `High`
- `Medium`
- `Low`
- `N/A`：只供 observation 使用

#### 判定狀態（`Status`）

判定狀態表示目前證據足以支持哪一種結論：

- `發現`：原始證據已足以支持問題成立。
- `待複驗`：現有證據不足，必須取得更多證據才能採信或排除。
- `observation`：只證明配置、屬性或外部關聯，尚未構成正式弱點。
- `排除`：證據顯示為誤報，或複驗結果安全。

#### 複驗狀態（`Verification`）

複驗狀態表示是否已由人員重新操作並確認結果：

- `已複驗`
- `未複驗`
- `不適用`：用於沒有可執行複驗的 observation 或排除項目

`Critical`／`High` 不強制人工複驗。任何風險等級都依證據是否充分來決定判定狀態：

- 原始證據足以支持結論時，判定狀態可以標為「發現」，複驗狀態則依實際情況填寫。
- 原始證據不足時，判定狀態標為「待複驗」。
- 未經人工複驗的正式發現仍納入風險等級統計，摘要另外列出未複驗數量。

### 5.2 自動分類與降等

下列項目一律標為 `observation`，風險等級填 `N/A`：

- 工具回報的原始風險等級為 `info`。
- `matched-at` 指向第三方網域。
- 證據只能證明屬性，例如偵測到外部租戶或使用中的技術。

下列項目先將工具回報的風險等級降低一級，再依證據是否充分判定狀態：

- ZAP 被動掃描的 Medium 配置缺失。
- Nuclei 的 CVE template 命中。

同一候選發現同時符合多條降等規則時，最多自動降低一級。其他不確定性寫入判斷理由，並以判定狀態或複驗狀態表示，不再重複降等。

### 5.3 採信條件

- 工具命中本身只建立候選，不保證問題成立。
- 結果直接包含受影響目標、問題行為與回應證據時，可以在未人工複驗的情況下標為「發現」，但複驗狀態必須填「未複驗」。
- 只有版本推定、模板名稱或不完整回應時，標為「待複驗」。
- CVE 候選沒有相符產品版本時不得採信為正式發現。
- 被動掃描的安全標頭缺失可用 `curl -sI <url>` 複驗；未執行時保留「未複驗」標示，不得假稱已確認。

### 5.4 統計規則

- `Critical`、`High`、`Medium`、`Low` 的統計只包含判定狀態為「發現」的項目。
- 摘要另列「未複驗的正式發現」數量，並按風險等級分組。
- `待複驗`、`observation` 與 `排除` 分開計數，不得加入正式弱點總數。
- 同一主機、同一問題的多個命中位置只計一項。
- 同類問題影響多個 IP 時，每個 IP 各計一項；全機摘要可另外說明「同類問題影響 N 個 IP」。

## 6. 交叉核對規則

### 6.1 同一主機內

- reachability 顯示 succeeded 的埠與 nmap open 埠不一致時，以 nmap 結果為資產盤點依據，並記錄不一致。
- 應進 Web scope 的 IP 若 Nuclei 與 ZAP 都沒有結果，且 `progress.log` 有 `Skipped`，結論是「未涵蓋」。
- Nuclei 回報產品或 CVE 時，對照 nmap 的產品與版本。
- ZAP 回報安全標頭缺失時，可用 `curl -sI <url>` 複驗。

### 6.2 不同主機間

- 同類問題影響多個 IP 時，摘要只描述一次問題類型與影響數量。
- 每個 IP 保留自己的 Finding ID、證據位置、修補狀態與複測結果。
- 不得因其中一個 IP 完成修補或複測，就更新其他 IP 的判定狀態。

## 7. Finding ID 與證據

### 7.1 Finding ID

以下規則必須遵守：

- Finding ID 代表發現本身，不代表排序位置。
- 風險等級、判定狀態、章節順序或修補狀態改變時，不得更換 ID。
- 同類問題影響多個 IP 時，每個 IP 使用不同 ID。
- 同一主機、同一問題的多個命中位置共用一個 ID。
- 複測時沿用原 ID；已有舊報告時，不得重新編號。

原始文件沒有規定 ID 格式。沒有既有格式時，建議使用不含風險等級的固定流水號，例如 `F-001`。ID 一旦建立，便不得因排序改變而重編。

### 7.2 證據位置（`Evidence`）

證據位置只保存檔案路徑，以及可再次找到該筆資料的欄位或識別碼。不要貼上整段 `request` 或 `response`。

- Nuclei：`outputs/<RUN_ID>/nuclei/nuclei.jsonl`，加上 `template-id`、`host` 與 `matched-at`。
- ZAP：`outputs/<RUN_ID>/zap/zap_report.html#<alert-id>`。
- nmap：`outputs/<RUN_ID>/nmap/safe_*.nmap` 的版本列，或 `vulners_*.nmap` 的 CVE 列。
- reachability：`outputs/<RUN_ID>/reachability/reachability.txt` 的主機區塊。
- progress：`outputs/<RUN_ID>/run/progress.log` 的警告行。

只有檔案內容穩定不變時才使用行號。合併或重新產生後行號可能改變的 JSONL，應優先使用欄位組合作為定位資訊。

複驗資料放在完整發現的「複驗」段落，包含：

- 複驗狀態。
- 重現指令或操作。
- 預期結果。
- 實際結果或「未執行」。
- `request` 與 `response` 的最短必要摘要。

## 8. 內部分析報告

內部報告保存候選、判斷理由、排除項目與限制，是交付稿的唯一整理來源。

### 8.1 固定結構

```markdown
# 內部分析報告（<RUN_ID>）

## 1. 範圍核對
- 原定目標：<依 targets.txt>
- 實際完成狀態：<完成、部分完成、未回應、未執行、無法驗證>
- 材料缺口：<無則填無>

## 2. 全機摘要
- 正式發現：Critical <N>、High <N>、Medium <N>、Low <N>
- 未複驗的正式發現：Critical <N>、High <N>、Medium <N>、Low <N>
- 待複驗：<N>
- observation：<N>
- 排除：<N>

## 3. <主機名稱或網域>（<實際掃描 IP>）

### 3.1 主機概況
- 角色：<依 env.md；未知填待確認>
- 識別資訊：<IP、網域、rDNS、其他解析 IP>
- 開放埠與服務：<埠、服務、版本、是否進 Web scope>
- 掃描狀態：<完成、部分完成、未回應、未執行、無法驗證>

### 3.2 發現索引
| Finding ID | 風險等級 | 發現名稱 | 判定狀態 | 複驗狀態 | 證據位置 |
| --- | --- | --- | --- | --- | --- |

### 3.3 完整發現
#### <Finding ID> <Finding 名稱>
- 受影響資產：<IP 與識別名稱>
- 證據來源：<工具或其他證據來源>
- 工具回報的風險等級：<工具原始值>
- 調整後的風險等級：<調整後值>
- 判定狀態：<發現、待複驗、observation、排除>
- 複驗狀態：<已複驗、未複驗、不適用>
- 問題說明：<證據能支持的問題描述>
- 受影響位置：<路徑或服務>
- 證據位置：<檔案路徑與定位資訊>
- 判斷理由：<採信、降等或排除理由>
- 可能影響：<問題成立時可能造成的影響>
- 修補建議：<可執行的處置；無須處置時填無>
- 複驗內容：<方法、預期結果、實際結果或未執行>

### 3.4 該機限制事項
- <警告、被略過目標、覆蓋缺口；無則填無>

## 4. 跨主機彙整
- <同類問題、影響 IP 數與 Finding ID 索引；無則填無>

## 5. 整體限制事項
- <DNS 多 IP、材料缺口、工具錯誤、未執行工作；無則填無>

## 6. 複測追蹤
| Finding ID | 負責人 | 處理內容 | 複測結果 |
| --- | --- | --- | --- |
```

每個實際掃描 IP 重複建立第 3 節。章節號可依主機數量順延，Finding ID 不跟著章節號改變。

## 9. 交付稿

交付稿只呈現本次實際執行的範圍，以及內部報告已完成判定的內容。它同時供管理者判斷整體風險，以及供技術人員修補與複測。

### 9.1 納入規則

| 內部報告的判定狀態 | 交付稿處理 |
| --- | --- |
| 發現 | 納入正式弱點摘要與完整弱點說明 |
| 待複驗 | 納入「待複驗項目」，不得計入正式弱點統計 |
| observation | 納入獨立的「觀察事項」，風險等級顯示 `N/A` |
| 排除 | 不納入交付稿，只留在內部報告 |

ZAP 被動掃描可列入「被動滲透測試」內容，但必須遵守以下規則：

- 只有本次實際執行且屬於約定範圍時才列入。
- 明確標示為被動掃描，不得描述成主動利用或攻擊驗證。
- 完整發現只在弱點說明維護一次；被動滲透測試章節只列測試範圍、限制與 Finding ID 索引。

### 9.2 固定結構

```markdown
# 資安檢測報告

## 1. 執行摘要
- 受測資產與完成狀態
- 正式發現的風險等級統計
- 未複驗的正式發現數量
- 待複驗項目數量
- 會影響結論的限制

## 2. 檢測範圍
- 只列本次實際執行的工作、目標與日期

## 3. 受測主機及資產盤點
| Role | IP | 開放埠與服務 | Result |
| --- | --- | --- | --- |

## 4. 工具與版本
| 工具 | 版本 | 版本證據 |
| --- | --- | --- |

## 5. 弱點摘要
| Finding ID | 風險等級 | 受影響資產 | 發現名稱 | 判定狀態 | 複驗狀態 |
| --- | --- | --- | --- | --- | --- |

依 `Critical`、`High`、`Medium`、`Low` 排列，只列判定狀態為「發現」的項目。

## 6. 完整弱點說明
### <Finding ID> <Finding 名稱>
- 風險等級：<Critical、High、Medium、Low>
- 受影響資產：<IP 與識別名稱>
- 判定狀態：發現
- 複驗狀態：<已複驗或未複驗>
- 問題說明：<問題與成立證據>
- 可能影響：<實際可能造成的影響>
- 證據位置：<檔案路徑與定位資訊>
- 修補建議：<可執行的修補方式>
- 複驗內容：<方法、結果或未執行>

## 7. 待複驗項目
| Finding ID | 候選風險等級 | 受影響資產 | 缺少的證據 | 下一步 |
| --- | --- | --- | --- | --- |

## 8. 觀察事項
| Finding ID | 受影響資產 | 觀察結果 | 可能影響 | 改善建議 | 證據位置 |
| --- | --- | --- | --- | --- | --- |

`observation` 不指定 `Critical`、`High`、`Medium` 或 `Low`，也不加入正式弱點統計。

## 9. 惡意程式檢測
- 只有本次實際執行且有 ClamAV 或 EDR 證據時保留本節
- 引擎與特徵碼版本
- 掃描類型
- 偵測結果
- 證據位置

## 10. 被動滲透測試
- 只有本次實際執行且屬於約定範圍時保留本節
- 掃描模式與測試範圍
- 未涵蓋的主動測試與限制
- Finding ID 索引

## 11. 修補建議與複測
| Finding ID | 負責人 | 處理內容 | 複測結果 |
| --- | --- | --- | --- |

## 12. 限制事項
- 未執行的約定工作與原因
- 被阻擋的服務或無法取得的憑證
- 未掃描的 IP、排除的目標與被禁止的測試方法
- 工具錯誤、覆蓋不足與缺少材料
```

若沒有惡意程式檢測或被動滲透測試輸出，刪除對應的結果章節，並在限制事項說明未執行或無法驗證。不得保留空白章節，也不得以 `none` 表示沒有風險。
